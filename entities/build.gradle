apply plugin: 'java'
apply plugin: 'kotlin'
apply plugin: 'kotlin-kapt'

dependencies {
    rootProject.ext.entitiesDeps.each {
        add(it.configuration, it.dependency, it.options)
    }
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
